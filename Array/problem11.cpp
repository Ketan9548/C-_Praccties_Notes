#include <iostream>
using namespace std;
void rotedarray(int arr[], int n){
    int a = 0;
    int b = n;
    int m;
    int temp[m];
    int mid = (a+b)/2;
    for (int i = 0; i < n; i++)
    {
        if(mid == arr[i]){
            temp[m] = mid;
        }
    }
    
}
int main()
{
    int arr[] = {1,2,3,4,5,6,7};
    int n = sizeof(arr)/sizeof(arr[0]);

    return 0;
}