#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> v = {1,2,3,4};
    vector<int> v1;
    for (int i = 0; i < v.size()-1; i++)
    {
        if (i = v.end())
        {
            v[i] = v[i]+1;
        }
        
    }
    v1 = v[i];
    for (int i = 0; i < v[i].size-1; i++)
    {
        
    }
    
    cout<<v1<<endl;
    
    return 0;
}